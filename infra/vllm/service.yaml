apiVersion: v1
kind: Service
metadata:
  name: vllm-server
  namespace: inference
  labels:
    app: vllm-server
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: vllm-server

